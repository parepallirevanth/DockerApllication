server {
  listen 80;
  server_name 192.168.123.14;
  location = /favicon.ico { access_log off; log_not_found off; }
  location /static/ {
      root /home/chatuser/AnsibleTestApp/chatapplication;
  }
  location / {
      include proxy_params;
      proxy_pass http://unix:/home/chatuser/AnsibleTestApp/chatapplication/chatapp.sock;
  }
}

